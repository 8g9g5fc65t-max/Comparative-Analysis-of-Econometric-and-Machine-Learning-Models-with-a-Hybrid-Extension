# TFM — Dynamic Volatility and Market Risk Forecasting
#
# Requires Python 3.9+ (developed/pinned against 3.11.14, in a dedicated
# venv at .venv/ — see README.md "Setup"). Earlier sessions ran this on
# Python 3.7.4 with legacy-compatible pins (yfinance==0.2.55,
# multitasking==0.0.11); that workaround is gone now that a modern Python
# is in use, in favor of current library versions across the board.

yfinance==1.5.2
multitasking==0.0.12
pandas_market_calendars==5.4.0
pandas==3.0.5
numpy==2.4.6
arch==8.0.0
scikit-learn==1.9.0
xgboost==3.2.0
matplotlib==3.11.1
